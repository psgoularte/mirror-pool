//! `mirror-pool` developer CLI.
//!
//! Subcommands (SPEC §4.5), landing across later milestones:
//! `keygen`, `deposit`, `prove`, `execute`, `disclose`, `sim`.

fn main() {
    eprintln!(
        "mirror-pool CLI: not yet implemented (subcommands land in milestones 2–8). \
         See SPEC.md §4.5."
    );
    std::process::exit(1);
}
